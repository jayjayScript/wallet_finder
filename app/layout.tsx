import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "WalletFinder — Discover Wallets Linked to Any Email",
  description:
    "Instantly scan 20+ crypto exchanges and Web3 wallets to discover which accounts are linked to any email address.",
  keywords: [
    "wallet finder", "crypto wallet lookup", "email wallet search",
    "coinbase binance metamask trust wallet",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
